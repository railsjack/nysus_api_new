# Use the README file as a reference to customize settings.

Favorite:
  fields:
    default: user_id, establishment_id
    form: user_id, establishment_id
  relationships: 
  application: Application
